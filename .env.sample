# Variables for common deployment settings (used in docker-compose.sample.yml)
# These replace the hardcoded <GCP project> and <Meta ID> placeholders.

# GCP Project ID for Google Cloud Logging
GCP_PROJECT=your-gcp-project-id

# Metadata ID for Google Cloud Logging (usually identifying the environment or deployment)
GCP_META_ID=your-meta-id