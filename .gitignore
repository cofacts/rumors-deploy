volumes/db-production/*
volumes/db-staging/*
volumes/db-sample/*
!.gitkeep
docker-compose.yml

# For mongodump output directory
db-backup/*
!db-backup/.gitkeep
data
env-files

# Prevent google-github-actions/auth credentials being committed to git by accident
gha-creds-*.json